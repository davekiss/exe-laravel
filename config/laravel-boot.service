[Unit]
Description=Laravel boot (run migrations on startup)
After=postgresql.service
Requires=postgresql.service

[Service]
Type=oneshot
User=exedev
ExecStart=/usr/local/bin/laravel-boot.sh

[Install]
WantedBy=multi-user.target
